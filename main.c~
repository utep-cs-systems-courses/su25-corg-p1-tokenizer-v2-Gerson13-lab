#include <stdio.h>
#include "tokenizer.h"
#include "history.h"


int main(){
  char input[] = "hello world this is a test";
  char **tokens = tokenize(input);

  printf("tokens:\n");
  print_tokens(tokens);

  free_tokens(tokens);

  return 0;
}
